datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Article {
  id          String   @id @default(cuid())
  title       String
  description String
  content     String
  image       String
  createdAt   DateTime @default(now())
}
